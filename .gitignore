# Dependencies
node_modules/

# Server runtime data — NEVER commit: contains the admin key and the
# customer-message inbox database.
server/data/

# Site image store (runtime content, written by the admin from the browser)
.image-slots.state.json

# Local working artifacts
scratch/
screenshots/
uploads/
.thumbnail
